services:
  cv:
    image: caddy:2.10.0-alpine
    container_name: web
    restart: unless-stopped
    volumes:
      - ./docker/web/Caddyfile:/etc/caddy/Caddyfile
      - ./dist/web/site:/usr/share/caddy/site
      - cv_caddy_data:/data
      - cv_caddy_config:/config
    networks:
      - external-network

volumes:
  cv_caddy_data:
  cv_caddy_config:

networks:
  external-network:
    external: true
